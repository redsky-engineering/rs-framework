import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from './Button';

<Meta title="RedSky/Button" component={Button} />

# Button Component

The `Button` component requires the user to create a themes/button.scss file and style the button there.
You can follow the scss layout for the button to get the correct button styles.

### Prop Interface

<ArgsTable of={Button} />

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <Button {...args} />;

## Button Contained Primary

<Canvas>
	<Story
		name="Button Contained Primary"
		args={{
			look: 'containedPrimary',
			type: 'button',
			children: 'Contained Primary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

## Button Contained Secondary

<Canvas>
	<Story
		name="Button Contained Secondary"
		args={{
			look: 'containedSecondary',
			type: 'button',
			children: 'Contained Secondary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

## Button Text Primary

<Canvas>
	<Story
		name="Button Text Primary"
		args={{
			look: 'textPrimary',
			type: 'button',
			children: 'Text Primary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

## Button Text Secondary

<Canvas>
	<Story
		name="Button Text Secondary"
		args={{
			look: 'textSecondary',
			type: 'button',
			children: 'Text Secondary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

## Button Outlined Primary

<Canvas>
	<Story
		name="Button Outlined Primary"
		args={{
			look: 'outlinedPrimary',
			type: 'button',
			children: 'Outlined Primary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>

## Button Outlined Secondary

<Canvas>
	<Story
		name="Button Outlined Secondary"
		args={{
			look: 'outlinedSecondary',
			type: 'button',
			children: 'Outlined Secondary'
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
